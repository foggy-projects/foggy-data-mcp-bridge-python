# v1.4 + v1.5 Overall Progress Closeout

## 文档作用

- doc_type: progress / closeout
- intended_for: execution-agent / reviewer / signoff-owner
- purpose: 汇总 Python v1.4/v1.5 及 Java 对齐参照的当前完成状态、测试证据、遗留项与下一步建议

## 基本信息

- version: v1.4 + v1.5
- status: current-closeout
- owning_repo: `foggy-data-mcp-bridge-python`
- java_timeWindow_repo: `foggy-data-mcp-bridge-wt-dev-compose`
- closeout_date: 2026-04-28
- delivery_mode: single-root-delivery
- experience: N/A，当前收口范围均为后端 DSL / SQL engine / schema docs，无 UI 交互面

## Scope

本记录覆盖以下已交付范围：

| Area | Python Status | Java Reference | Current Conclusion |
|---|---:|---|---|
| v1.4 fsscript `in` / `not in` | signed-off | Java fsscript operator behavior | aligned for signed-off subset |
| v1.5 Phase 1 dialect functions + arity | signed-off | Java dialect function translation tests | aligned for signed-off subset |
| v1.5 Phase 2 calculatedFields dependency graph | signed-off | Java calculated field dependency / cycle behavior | aligned for signed-off subset |
| v1.5 Phase 3 AST visitor path | signed-off with non-blocking risks | Java fsscript AST + SQL visitor architecture | architecture-aligned, default flag remains conservative |
| Python timeWindow parity lane | signed-off with risks, follow-ups closed | Java 8.3.0.beta timeWindow acceptance | aligned for rolling / cumulative / comparative signed-off subset |
| `timeWindow + calculatedFields` | signed-off accepted | Java 8.4.0.beta contract + 8.5.0.beta runtime fixtures | aligned for post scalar calculatedFields subset |
| CTE / compose query baseline | documented as accepted-with-risks in Java G10 and covered by Python compose tests | Java 8.3.0.beta compose / CTE design | aligned for accepted baseline; explicit named CTE and recursive CTE remain non-goals |

## Development Progress

- [x] v1.4 `in` / `not in` 已完成并独立签收。
- [x] v1.5 Phase 1 / Phase 2 / Phase 3 已完成版本级签收，结论为 `accepted-with-risks`。
- [x] Python timeWindow parity lane 已完成 S4，覆盖 DTO / MCP passthrough、validator、rolling/cumulative/comparative SQL expansion、SQLite / MySQL8 / Postgres 实库矩阵。
- [x] MySQL8 demo 2025 sales fact seed 已补齐，并验证 yoy prior / diff / ratio 非空。
- [x] `timeWindow + calculatedFields` 已按 Java 8.4.0.beta 契约和 8.5.0.beta fixture 对齐，实现后置 scalar calculatedFields。
- [x] post scalar calculatedFields 支持 `alias` 输出场景下继续用计算字段 `name` 做 `orderBy.field`，最终 SQL 映射到输出 alias。
- [x] LLM-facing `query_model_v3` schema / descriptions 已同步 `timeWindow` 联用规则。
- [x] 质量闸门、覆盖审计、功能签收均已完成并回写进度文档。

## Current Two-Engine Alignment

### CTE / Compose

当前对齐结论：

- Java 8.3.0.beta 设计文档已记录 CTE / 派生 / Join / Union 双端实施完成，包含自动 CTE 去重与 compose query plan-aware 编译路线。
- Python repo 已有 compose / CTE 相关编译和 MCP tool 测试，当前收口未发现新的阻断项。
- 显式命名 CTE、递归 CTE 仍不属于当前签收范围；跨数据源 live detection 已在 post-v1.5 Stage 1 完成 Python / Java 双端实现。

保留项：

- F-7 cross-datasource live detection 已完成 Python / Java 双端实现；Python 原 `xfail` 已转 regular pass，Java 镜像提交 `f918343` 已推送 `dev-compose`。
- 如后续需要把 CTE 能力从 accepted baseline 扩到显式 named CTE / recursive CTE，需要新开契约与双端 follow-up。

### TimeWindow

当前对齐结论：

- Java 8.3.0.beta `timeWindow` 已签收，Python v1.5 parity lane 已完成运行时对齐。
- Java 8.4.0.beta 明确 `timeWindow + calculatedFields` 契约，Java 8.5.0.beta 已实现后置 scalar calculatedFields；Python 已按该子集完成实现和验收。
- 当前两个引擎对文档要求内的 rolling / cumulative / comparative / post scalar calculatedFields 子集已对齐。

当前签收的允许能力：

- `timeWindow` 输出列之上的后置 scalar calculatedFields。
- 表达式引用 timeWindow 输出列，例如 `salesAmount__prior`、`salesAmount__diff`、`salesAmount__ratio`、`salesAmount__rolling_7d`。
- 最终层 `ORDER BY` / `LIMIT`，并支持按 post calc `name` 排序、输出使用 `alias`。

当前签收的 fail-closed 能力：

- `targetMetrics` 不允许引用请求级 calculatedFields。
- 后置 calculatedFields 不允许 `agg`。
- 后置 calculatedFields 不允许 `partitionBy` / `windowOrderBy` / `windowFrame`。
- 后置表达式只能引用 timeWindow 最终输出列。

## Testing Progress

最新执行证据：

| Command | Result | Purpose |
|---|---:|---|
| `python -m pytest tests\test_dataset_model\test_time_window_java_parity_catalog.py tests\test_dataset_model\test_time_window_sqlite_execution.py -q` | 22 passed | Java fixture catalog + SQLite runtime |
| `python -m pytest tests\test_dataset_model\test_time_window.py tests\test_dataset_model\test_time_window_sqlite_execution.py tests\test_dataset_model\test_time_window_java_parity_catalog.py tests\test_mcp\test_java_alignment.py tests\test_dataset_model\test_calc_field_dependency_e2e.py tests\test_dataset_model\test_semantic_service_formula_compiler.py tests\test_dataset_model\test_window_functions.py tests\test_dataset_model\test_sql_quoting_and_errors.py tests\test_dataset_model\test_conditional_aggregate_if_alignment.py -q` | 173 passed | timeWindow + calculatedFields + MCP / formula / quoting regression |
| `python -m pytest tests\integration\test_time_window_real_db_matrix.py -q` | 17 passed | SQLite / MySQL8 / Postgres real DB matrix |
| `python -m pytest -q` | 3316 passed | full Python regression |
| `python -m json.tool src\foggy\mcp\schemas\query_model_v3_schema.json` | passed | LLM-facing schema JSON validity |

已知非失败项：

- Java formula parity snapshot 已由 `FormulaParitySnapshotTest` 生成并落盘，Python strict Java/Python snapshot compare 已启用。
- Java F-7 mirror verification: focused compose lane 3 surefire lanes each 66 passed; compose lane 184 passed; `FormulaParitySnapshotTest` 3 surefire lanes each 5 passed.
- xfailed: none in Python full regression. Historical F-7 cross-datasource xfail has been resolved, and Java mirror is complete.

## Acceptance / Evidence Links

- v1.4 acceptance: `docs/v1.4/acceptance/P2-fsscript-in-notin算子对齐Java-acceptance.md`
- v1.5 version signoff: `docs/v1.5/acceptance/version-signoff.md`
- v1.5 coverage audit: `docs/v1.5/coverage/v1.4+v1.5-coverage-audit.md`
- timeWindow parity acceptance: `docs/v1.5/acceptance/P1-timeWindow-Python-parity-acceptance.md`
- timeWindow calculatedFields quality gate: `docs/v1.5/quality/P1-timeWindow-calculatedFields-implementation-quality.md`
- timeWindow calculatedFields coverage audit: `docs/v1.5/coverage/P1-timeWindow-calculatedFields-coverage-audit.md`
- timeWindow calculatedFields acceptance: `docs/v1.5/acceptance/P1-timeWindow-calculatedFields-acceptance.md`
- real DB matrix progress: `docs/v1.5/P1-timeWindow-real-db-integration-matrix-progress.md`
- post-v1.5 Stage 1 progress: `docs/v1.5/S1-F7-datasource-identity-contract-progress.md`
- post-v1.5 Stage 2 progress: `docs/v1.5/S2-formula-parity-snapshot-ci-progress.md`
- post-v1.5 Stage 1/2 quality gate: `docs/v1.5/quality/S1-S2-post-v1.5-followup-implementation-quality.md`

## Remaining Gaps / Not Yet Aligned

以下均不是当前 signed-off subset 的阻断项：

| Gap | Status | Suggested Owner / Timing |
|---|---|---|
| Python fsscript parser 支持 `IS NULL` / `BETWEEN` / `LIKE` / `CAST AS` 等 SQL-specific 关键字走 AST | Phase 4 optional | 仅在决定默认启用 AST path 前立项 |
| `+` 运算符按操作数类型区分字符串拼接与数值加法 | Phase 4 optional | 与 AST default flip 一起评估 |
| Formula parity Java snapshot CI 固化 | complete-with-ci-assumption | Java `FormulaParitySnapshotTest` 已可生成本地 snapshot；Python strict compare、schema/catalog/integrity drift detection 已启用；外部 CI workflow 接线仍待未来 CI 配置 |
| Java/Python normalized SQL golden diff harness | infra gap | 后续作为跨引擎回归基建独立立项 |
| SQL Server timeWindow real DB matrix | complete | Stage 4 已复用 Java demo SQL Server 2022 环境补齐 11 项真实库测试 |
| timeWindow calculatedFields 输出别名投影 Java/Python 对齐 | pending | Stage 5 独立契约项；Python 当前允许 calc alias in columns，Java `SchemaAwareFieldValidationStep` 暂未放开 |
| timeWindow 后置二次聚合 / 二次窗口 | not implemented | 等 Java 契约明确后新开 follow-up |
| calculatedFields 作为 timeWindow targetMetrics 输入 | intentionally unsupported | 当前 Java / Python 均 fail-closed |
| explicit named CTE / recursive CTE | non-goal | 如业务需要，先补双端契约 |

## Experience Progress

- status: N/A
- reason: 本收口范围是 DSL / SQL engine / schema docs，无页面、表单、权限可见性或前端交互变化。

## Closeout Decision

- v1.4 / v1.5 主线：已签收。
- timeWindow parity follow-up：已签收，既有风险均有记录。
- `timeWindow + calculatedFields` follow-up：已签收 accepted。
- 当前两个引擎在文档定义的 CTE baseline 与 timeWindow signed-off subset 上达到对齐；剩余项均为明确的非目标、长期基础设施或未来契约扩展。

## Recommended Next Steps

1. 按 `docs/v1.5/P2-post-v1.5-followup-execution-plan.md` 进入 post-v1.5 follow-up 队列。
2. 若目标是继续做基建，推进 normalized SQL golden diff harness。
3. 若 Java 后续扩展 `timeWindow + calculatedFields` 二次聚合/窗口语义，Python 再按 Java 契约新开 follow-up。
