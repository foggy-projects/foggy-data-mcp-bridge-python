{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Execute a compose script to orchestrate multiple query models",
  "properties": {
    "script": {
      "type": "string",
      "description": "FSScript code for dataset.compose_script. Use the AI-facing SemanticDSL style: dsl({...}) for base/derived plans, plus .join(...) and .union(...) for plan composition. End query plans by returning an envelope such as return { plans: plan }; do not call .execute() directly unless specifically instructed."
    }
  },
  "required": ["script"]
}
